interface ThumbnailItem {
    title: string;
    description: string;
}

// This represents the structure of the data that is stored in the projects.json file.
interface ProjectItem {
    name: string;
    scan_location: string;
    description: string;
}